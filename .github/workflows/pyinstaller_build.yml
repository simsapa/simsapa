name: PyInstaller Build

on:
  push:
    branches:
      - main
      - develop
    tags:
      - 'v[0-9].*'

jobs:
  briefcase:
    name: Bundle on ${{ matrix.os }} / ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-13, windows-2022]
        python-version: ["3.11"]
        arch: ["x86_64"]
      fail-fast: false
    defaults:
      run:
        shell: bash
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      - name: Install Node
        uses: actions/setup-node@v4
        if: runner.os == 'MacOS'
        with:
          node-version: 20

      - name: Install Node libs
        if: runner.os == 'MacOS'
        run: npm install --global create-dmg

      - name: Install Python pip
        run: python -m pip install --upgrade pip

      - name: Install Python libs
        run: |
          pip install pyinstaller
          pip install -r requirements.txt

      - name: Mac Build .app
        if: runner.os == 'MacOS'
        run: |
          pyinstaller run.py --name "Simsapa Dhamma Reader" --noconfirm --onedir --windowed --clean --noupx -i "simsapa/assets/icons/appicons/simsapa.ico" --add-data "simsapa/assets:simsapa/assets" --add-data "simsapa/alembic:simsapa/alembic" --add-data "simsapa/alembic.ini:simsapa/alembic.ini" --target-architecture ${{ matrix.arch }} --osx-bundle-identifier 'reader.dhamma.simsapa' --hidden-import=tiktoken_ext --hidden-import=tiktoken_ext.openai_public

      - name: Mac Build .dmg
        if: runner.os == 'MacOS'
        run: ./scripts/build_dmg.sh

      # NOTE: Windows uses \ in paths and expects ; not : in data names
      - name: Windows Build .exe
        if: runner.os == 'Windows'
        run: |
          pyinstaller run.py --name "Simsapa Dhamma Reader" --noconfirm --onedir --windowed --clean --noupx -i "simsapa\assets\icons\appicons\simsapa.ico" --add-data "simsapa\assets;simsapa\assets" --add-data "simsapa\alembic;simsapa\alembic" --add-data "simsapa\alembic.ini;simsapa\alembic.ini" --hidden-import=tiktoken_ext --hidden-import=tiktoken_ext.openai_public

      - name: Upload Artifact for MacOS
        uses: actions/upload-artifact@v3
        if: runner.os == 'MacOS'
        with:
          name: simsapa-${{ github.ref_name }}-${{ matrix.os }}-${{ matrix.arch }}.dmg
          path: 'dist/*.dmg'

      - name: Upload Artifact for Windows
        uses: actions/upload-artifact@v3
        if: runner.os == 'Windows'
        with:
          name: simsapa-${{ github.ref_name }}-${{ matrix.os }}
          path: 'dist/Simsapa Dhamma Reader/'
